# meta/main.yml
---
galaxy_info:
  role_name: galaxy_deployment_test   # must match your repo name (after the dot)
  author: digambarrajaram
  description: A role to deploy something via Ansible Galaxy
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy

dependencies: []
